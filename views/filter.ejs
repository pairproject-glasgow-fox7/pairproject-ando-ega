<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Performance Organizer</title>
</head>
<body>
    <%-include('./partials/nav')%>
    <div style="text-align: center;">
        <h4>Band</h4>
    </div><br>

    <table border="1" style="width: auto; margin-right: auto; margin-left: auto;">
        <thead>
            <tr>
                <th>Band Name</th>
                <th>Origin</th>
                <th>Band Member</th>
            </tr>
        </thead>
        <tbody>
            <% for(let i = 0; i < data.length; i++){ %>
                <tr>
                    <td> <%= data[i].band() %></td>
                    <td> <%= data[i].band_origin %></td>
                    <td> <%= member(data[i].band_member) %></td>
                </tr>
            <%}%>
        </tbody>
    </table>
</body>
</html>